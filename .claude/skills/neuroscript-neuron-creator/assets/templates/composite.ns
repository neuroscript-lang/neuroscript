# Composite Neuron Template
# Replace placeholders with actual values

neuron {{NeuronName}}({{param1}}, {{param2}}):
    in: [{{input_shape}}]
    out: [{{output_shape}}]
    graph:
        # Simple pipeline example
        in ->
            {{Layer1}}({{args}})
            {{Layer2}}({{args}})
            out
